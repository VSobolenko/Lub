#include <conio.h>
#include <stdio.h>

int main() {
	unsigned i,k,j;
	int max;
    int a[k]; 
    
    printf("k = ");
    scanf("%d", &k);

    for(j=0; i<k; j++){
        for(i=0; i<k; i++) {
            printf("a[%d] = ", i);
            scanf("%d", &a[i], &a[j]); 
        }
    } 
    return 0;
}

